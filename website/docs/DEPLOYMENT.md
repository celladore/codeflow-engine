# CodeFlow Website - Deployment Guide

Moved. This file used to duplicate the deployment guide and had drifted out
of sync with reality (wrong branch, wrong resource names, wrong domain, wrong
workflow filename) — that history is why this is now a pointer instead of a
second copy.

**See [`docs/WEBSITE_DEPLOYMENT.md`](../../docs/WEBSITE_DEPLOYMENT.md)** at
the repo root for the current, verified deployment guide (Azure resources,
custom domain setup, CI/CD workflow, troubleshooting).
